# -*- mode: sh; sh-shell: zsh; -*-

export MANPATH="/usr/local/man:$MANPATH"

export GPG_TTY=$(tty)

# This is really annoying because it always locks out Debbie
umask 002

# Make emacs play well locally
export EMACS_SOCKET_NAME=/tmp/derek/emacs1000/server

# Make admitting Mike into tmux simpler
mikemux() {
    chgrp family sessions/mojo
    chmod g+rwx sessions/mojo
}

cdpath+=/home/software/tools

path+=/home/software/scala/bin
path+=~/.dotnet/tools
export PATH


# Utility function for rotating PDFs
pdfrotate() {
    FILENAME=$(mktemp PDFROTATE.XXXXXXXX)
    qpdf --rotate=${1} ${2} ${FILENAME} && mv -f ${FILENAME} ${2}
}
