# -*- mode: sh; sh-shell: zsh; -*-

export MANPATH="/usr/local/man:$MANPATH"

export GPG_TTY=$(tty)

# This is really annoying because it always locks out Debbie
umask 002

# Make emacs play well locally
export EMACS_SOCKET_NAME=/tmp/derek/emacs1000/server

path+=/home/software/scala/bin
path+=~/.dotnet/tools
export PATH
